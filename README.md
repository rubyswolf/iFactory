# iFactory

iFactory by Dart Spark is an Electron desktop for Windows for generating iPlug2 audio plugin projects. 

## CLI
iFactory comes with a custom `ifact` CLI tool designed for AI agents to use to automate functions and print documentation.
If running from source it must be activated with `npm link`.

## Run

```powershell
npm install
npm start
```
